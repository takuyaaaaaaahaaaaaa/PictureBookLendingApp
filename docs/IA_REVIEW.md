# IA見直し：タップ数監査と再設計案

[DESIGN_PRINCIPLES.md](DESIGN_PRINCIPLES.md) の基準を物差しに、現行実装（2026-06時点）の
情報設計を監査した結果と、再設計の提案。

---

## 1. タップ数監査（実コードベース）

対象コード: `ContentView` → `BookListContainerView` → `LoanActionContainerButton` →
`LoanFormContainerView` / `LoanListContainerView`

### 貸出フロー：目標3タップ → **現状7タップ** ❌

絵本タブで対象の絵本を見つけた後（検索コストは除く）：

| # | 操作 | 実装箇所 |
|---|---|---|
| 1 | 行の「貸出」ボタン | `LoanActionContainerButton` → sheet |
| 2 | 組Pickerを開く | `LoanFormView` 組Picker |
| 3 | 組を選択 | 〃 |
| 4 | 利用者Pickerを開く | `LoanFormView` 利用者Picker |
| 5 | 利用者を選択 | 〃 |
| 6 | 「登録」ボタン | `LoanFormContainerView` bottomBar |
| 7 | 成功アラートの「OK」 | 〃 alert |

※保護者に貸す場合は利用者種別の切替で+1タップ（計8）。

### 返却フロー：目標2タップ → **現状3タップ** ⚠️

| # | 操作 | 実装箇所 |
|---|---|---|
| 1 | 行の「返却」ボタン | `LoanActionContainerButton` |
| 2 | 確認ダイアログ「返却する」 | 〃 alert |
| 3 | 成功アラートの「OK」 | 親Viewの alert |

### その他の基準チェック

| 基準 | 判定 | 備考 |
|---|---|---|
| 検索バー常時表示 | ✅ | `.searchable(displayMode: .always)`。ただし Container と Presentation の両方に二重定義あり（#143の原因候補） |
| 操作の取り消し（1タップ） | ❌ | 未実装（#38）。返却ミスのリカバリーは再貸出＝7タップかかる |
| 連続操作への最適化 | ❌ | 貸出フォームが毎回組選択から始まる。前回選択の記憶なし |
| 利用者起点の動線 | ❌ | すべて絵本起点。「この子の返却と貸出をまとめて」ができない |
| 延滞の可視化 | ✅ | 貸出管理タブにバッジ表示 |

---

## 2. 構造的な発見：動線がすべて「絵本起点」

現行IAは2タブとも **絵本→利用者** の順で操作が流れる。
しかしお迎えラッシュの実態は **親子（利用者）単位** でiPadの前に来る：

> 「Aちゃんが2冊返して、1冊借りる」

これを現行IAでやると：返却3タップ×2冊 + 絵本を探して貸出7タップ = **13タップ＋検索2回**。
主シーン（1組30秒以内）に対して、IAの主語が逆向きになっている。

---

## 3. 再設計案

### Phase 1：タップ削減（IA据え置き・即効性重視）

| 施策 | 効果 | 関連issue |
|---|---|---|
| 成功アラート廃止 → ハプティクス＋自動クローズ | 貸出・返却とも−1タップ | #18 |
| 返却の確認ダイアログ → 「元に戻す」スナックバーに置換 | 返却 3→**1タップ**＋Undo | #38 |
| 貸出フォームが前回選択した組を記憶 | 連続貸出時 −2タップ（※仮説検証中） | — |
| `.searchable` 二重定義の解消 | 検索・ソート不具合の解消 | #143 |

**Phase 1後：貸出 7→4タップ、返却 3→1タップ**（原則2「ミスは前提、リカバリーは1タップ」準拠）

> **※「組の記憶」は仮説検証中の機能。** 「お迎えは組ごとにまとまって来る」という
> 推測に基づく。実際にはきょうだいで別の組・お迎えがバラバラ等で組が連続しない
> 可能性があり、効果が出る頻度は未検証。ただし外れても無害（前回の組が選択済みに
> なるだけで利用者は未選択＝誤操作にならない）なため採用。Phase 2で利用者起点IAに
> 作り替える際、貸出フォーム自体が消える可能性があるので、そのとき残否を再判断する。

### Phase 2：利用者起点の動線を追加（IAの再構成）

> **⚠️ この節の具体案（組グリッド等）はインタビュー証拠により置き換え済み。**
> 最新の確定設計は [SCREEN_DESIGN_PHASE2.md](SCREEN_DESIGN_PHASE2.md)、経緯は §6 を参照。
> 以下は当初案の記録として残す。

タブ構成を「絵本」「貸出管理」から **「絵本」「利用者」** に再編する：

```
└ 利用者タブ（現・貸出管理タブを拡張）
  ├ 組の大ボタングリッド（もも組・ばら組…）…… 1タップ
  ├ 利用者一覧（あいうえお順・大きな行）……… 1タップ
  └ 利用者カード画面
    ├ 借りている絵本一覧 → 各行「返却」……… 1タップ/冊
    └ 「新しく借りる」→ 絵本選択 → 完了 …… 2タップ/冊
```

- 「Aちゃんが2冊返して1冊借りる」= 組1＋名前1＋返却2＋貸出2 = **6タップ**（現行13）
- 利用者を選んだ時点で組・名前の入力が不要になるため、貸出フォーム自体が消える
- 延滞バッジ・期限表示は利用者カードに集約（副シーンの俯瞰は組グリッドで担保）
- 絵本起点の動線（絵本タブ）は維持。両起点が揃って初めて現場の流れに合う

### Phase 3：磨き込み（Phase 2のIAが固まってから）

- UIカタログ整備（#40）・Liquid Glassの統一感・お祝いマイルストーン（#26）
- スリープモード（#117）：常設iPad前提の運用に合わせる

---

## 4. 音声入力の検討（WWDC26時点）

対象環境：11インチ iPad Air (M3)・iOS 27視野。

| 手段 | オフライン | 評価 |
|---|---|---|
| キーボードのマイク（音声入力） | ✅ | ゼロコストだが削減効果小 |
| Speech framework（iOS 26〜 SpeechAnalyzer/SpeechTranscriber） | ✅ 完全オンデバイス | 利用者200人・絵本500冊の**閉じた語彙**へのマッチングなので精度を稼ぎやすい |
| FoundationModels（iOS 27で音声理解対応・OS同梱） | ✅ 完全オンデバイス | 発話→`{組, 利用者, 絵本, アクション}` の構造化まで可能。M3なら性能十分 |
| Siri / App Intents（SiriKitは非推奨化） | △ intentごとにオンデバイス必須を宣言可 | クラウドルーティング経路は採用しない（オフライン要件・園児名のプライバシー） |

**方針：音声は主動線ではなく「検索の加速装置」。**

- お迎えラッシュの騒音（子どもの声）下では誤認識→言い直しの「音声渋滞」リスクがあり、
  行列ファースト原則により主動線はタップを維持
- Phase 2の利用者起点IAにマイクボタンを重ねる：「もも組のたろうくん」→利用者カード直行→確認1タップ
- 同名対策：認識結果は必ず候補表示し1タップで確定。実行前確認＋Undoは音声でも省略しない
- 園児の名前を扱うため音声処理は**オンデバイス限定**（クラウド送信不可）

## 5. 監査中に見つけた実装上の課題（デザインとは別軸）

- `LoanActionContainerButton:46` にデバッグ用 `Self._printChanges()` が残存
- 同ファイル72-76行にコメントアウトされたalertコード
- `BookListContainerView` と `BookListView` の `.searchable` 二重定義（#143の原因候補）
- `BookListContainerView` の `isEditMode` が常にfalse（トグルUIが存在しない）

---

## 6. インタビュー証拠とIA決定（2026-06-16）

[PERSONA.md](PERSONA.md) のユーザインタビュー（2026-03・60代女性）で得た証拠に基づき、
Phase 2設計を再評価して以下を決定した。設計本体は [SCREEN_DESIGN_PHASE2.md](SCREEN_DESIGN_PHASE2.md)。

| 証拠 | IAへの影響 | 決定 |
|---|---|---|
| 操作の約9割は保護者・祖父母。管理は保育士15人中1人 | 「主役＝先生」の前提が誤り | ペルソナの主役を保護者・祖父母に訂正（DESIGN_PRINCIPLES §1） |
| 発見5：他人の貸出が見えるのが嫌 | 全利用者の貸出を並べる一覧はNG | **一覧は名前＋「かりてます」印のみ**。詳細は自分の子の画面でだけ表示 |
| 発見2：園児/保護者の2段階選択が冗長 | 貸出フォームの種別選択が邪魔 | **種別選択UIを廃止**。利用者画面に園児枠＋おうちの人枠を常設2枠表示 |
| 発見6：2枠制が知られていない | 周知の仕組みが必要 | 2枠の常設表示が周知を兼ねる（Tips不要に） |
| 発見3・4：保護者向けの本もある／タブ名の粒度 | 「絵本」が実態と不一致 | **呼称を「図書」に変更**（日本語UIのみ。コードの`Book`は維持） |
| 発見1：文字が小さい | タイポグラフィ | DESIGN_PRINCIPLES §5 を主動線に適用（Phase 2 段取り5） |

### 決定に伴う旧設計の破棄

- 「全部見えるフラット利用者一覧」（Phase 2 当初案）：発見5に反するため破棄
- 「組グリッド」：タップ増のため破棄（先行して破棄済み）
- 「利用者カード（履歴付き詳細への遷移）」：主動線からは外し、管理用として設定内に存続

### 学び（IA設計の進め方）

IAが前進したのは推論を重ねたときではなく、**証拠が入ったとき**だった
（「1人1冊」の現場知識、インタビューのプライバシー懸念）。以後のIA変更は
「どの証拠に基づくか」を必ずこの表に追記してから行う。

### 追記：動詞ファーストへの転換（2026-06-16 同日）

上記の決定後、さらに以下の証拠で設計を更新した：

- **現場知識（オーナー）**：「借りる」と「返す」は本選びの時間を挟んだ**別々の用事**であり、
  同一画面に返却と貸出を同居させる利点が薄い。返す動線に無借用者を表示する意味もない
- **共用端末の定石**：ATM・券売機と同じキオスク型利用のため「最初に用事を選ぶ」動詞ファーストが適合
- **切替容易性（オーナー指摘）**：ホーム大ボタンよりタブの方が用事の行き来が常時1タップで速い

→ **タブ構成を「かえす（既定）／かりる／図書」に決定**。
「名前のみ一覧＋子の画面」のプライバシー設計は各動詞タブ内の部品として維持。
確定設計は [SCREEN_DESIGN_PHASE2.md](SCREEN_DESIGN_PHASE2.md)。

### 追記2：かりるの本起点化とタブ実装方針（2026-06-16 同日）

- **現場知識（オーナー）**：借りる人は棚で選んだ本を**手に持って**iPadに来る。
  また園児の枠が満杯でも保護者の枠で借りるケースがあり、枠の都合は最後にしか分からない
  → **かりるは本起点（本→名前→枠）に変更**。枠タップが実行確認を兼ねる
- 副作用として図書タブが不要になり（かりるタブの起点＝図書一覧が兼務）、**タブは2つ**に
- **タブバー実装（オーナー指摘：Liquid Glassはデフォルトに合わせたい）**：
  標準TabView（上部タブ）＋文字サイズ底上げで開始し、ペーパーテストで田中さん基準に
  不足と判明した場合のみコンテンツ内の大型切替（標準部品）にフォールバック。
  フルカスタムボトムバーは最終手段。**逸脱を先回りしない**

### 追記3：切替UIの確定（2026-06-16 同日）

- TabViewは使わず、**コンテンツ上部の大型切替（かえす／かりる）**に確定
- 根拠：発見7「一番触ってほしいところ大きく」／標準タブバーはDynamic Typeで育たない／
  **視線導線は上から下（オーナー指摘）**なので切替は一覧より上に置く
- タブバーの模造（カスタムボトムバー）はLiquid Glass逸脱のため不採用。
  タブバー非搭載＋標準部品の切替はHIG上合法で、逸脱にあたらない
- 戻り先：ペーパーテストで大型切替が過剰と分かれば標準TabViewへ

### 追記4：切替UIは標準TabViewに決定（2026-06-16 同日・最終）

- 大型切替（C案）はいったん採用したが、**未検証の推論（発見7の適用）のために
  システムタブバーの無料の挙動（表示・非表示・質感・状態管理）を捨てて
  自前管理コストを払うのは「逸脱を先回りしない」方針に反する**（オーナー判断）ため撤回
- **標準TabView＋「アプリごとの文字サイズ」最大化で開始**。既定タブ＝かえすなので
  タブ操作の頻度自体が低い
- ペーパーテスト・実機で「67歳基準で小さすぎる」証拠が出た場合のみC案（コンテンツ上部の
  大型切替）へ移行。A⇄Cは中身の2画面が共通で移行コストが低い

### 追記5：かえす一覧に保護者名も表示（2026-06-16 同日）

- **オーナー指摘**：親の名前も一覧に出してよいのでは
- 検討の結果採用。「子の名前だけ一覧」には、保護者が自分の借りた本を
  **自分の名前で探して見つからない**という隠れた失敗モードがあった
- `UserType.guardian(relatedChildId:)` の紐付けをコードで確認し、
  **どの家族名から入っても同じ「家庭の画面」（2枠）に着地**する形に統合。
  間違うドアがなく、家族2冊も1回の入場で返せる

### 追記6：俯瞰画面は作らず、かえすタブが兼ねる（2026-06-16 同日）

- **オーナー指摘**：先生の俯瞰もかえすタブでよいのでは
- 論点は「延滞している家の名前が他の保護者にも見えること」の許容可否だった
- **相反する証拠を両方記録する**：
  - 発見5（インタビュー）：「見えるのが嫌だなって話もちらっとあった」
  - 現場知識（オーナー）：園の**入園管理では名前一覧が全保護者に見える運用が既にある**。
    現行の絵本管理（貸出管理タブ）も誰が何を借りているか可視のまま、問題なく運用されている実績
- オーナー判断で**許容**。かえすタブに「きげんすぎ」マーク＋「きげんすぎのみ」フィルタを置き、
  **「設定 > 貸出状況」は作らない**（画面が1つ減る）。
  ただし書名・期限は一覧に出さず家庭の画面まで（発見5への配慮はこの線で維持）
- 将来「なぜ延滞が見えるのか」と迷ったらこの記録に戻ること

### 追記7：HIG確認と延滞数タブバッジの廃止（2026-06-16 同日）

- **オーナーの不安**：「HIGではタブはタスクではなくオブジェクトがよいという話がなかったか」
- HIG原文（Tab bars）を確認。禁止は「アクションを実行するタブ」であり、
  機能セクションを表すタブは合法（公式例＝時計アプリのアラーム/ストップウォッチ/タイマー）。
  「かえす/かりる」はセクション表示のナビゲーションなので**HIG準拠**と結論
- 原文確認の副産物として、HIG "Reserve badges for critical information" に照らし
  **現行の延滞数タブバッジ（`.badge(overDueCount)`）をPhase 2で廃止**と決定。理由3点：
  1. **常時点灯になる**：200家庭運用で延滞ゼロの瞬間はほぼなく、点きっぱなしの警告は
     情報量ゼロ（HIGの言う impact の希釈）
  2. **宛先と表示先のズレ**：バッジの宛先は管理担当の先生1人だが、iPadの前に立つのは
     9割が保護者。他家の延滞集計は保護者には行動可能性ゼロ
  3. **集計値は自分事にならない**：延滞中の家庭が見ても「園全体で7件」からは気づけない
- 先生向けの受動通知が将来必要になったら、正しいチャネルは **#132（Widgetに貸出数表示）**
  （先生の見る場所に先生宛の情報を出す＝宛先と表示先が一致する）

### 追記8：UI文言はひらがなにしない（2026-06-16 同日）

- **前提修正（オーナー）**：端末を操作するのは**保護者のみ**。園児は操作しない
- したがってUI文言を園児向けにひらがな化する意味はなく、むしろ大人（67歳基準含む）には
  ひらがなの連続は漢字表記より読み取りが遅い。**通常の大人向け表記に統一**：
  - タブ名：「返却」「貸出」（図書館用語として既知・TERMS準拠・発見4の粒度も名詞で統一）
  - フィルタ：「延滞のみ」／空状態：「現在、貸出中の利用者はいません」
  - 枠名：「園児の本」「保護者の本」
- **園児の名前がひらがななのはデータ登録の運用**（園ではすべてひらがな入力）であり、
  UI文言の話とは別。名前表示はデータのまま

### 追記9：ペーパーテスト省略と「家庭の画面＝コンポーネント」（2026-06-16 同日）

- **ペーパーテストは実施しない**（オーナー判断）。根拠：
  1. 現行UIで60代の被験者が**説明なしで操作を完了した実績**がある（発見8「シンプルだもん」「詰まりはしない」）。Phase 2は操作の原子（名前や本をタップ→ボタン）を変えない
  2. 最大の検証対象だった切替UIは**A⇄Cの移行コストが低い設計**のため、事前検証の保険料が見合わない
  3. 被験者の時間調整こそが最大コストなので、その1回は**動くPhase 2ビルドの実機テスト**（段取り6・台本＝PAPER_TEST_PHASE2.md）に投資する
- **「家庭の画面」の再確認**（オーナー問答）：返却だけなら本人の枠だけで足りるが、
  消しても画面数・タップ数の節約はゼロ（本の確認画面は必要）。一方、貸出時の
  **満枠→隣の空き枠リカバリー**（オーナー自身の体験シナリオ）と**発見6の周知**は
  家庭単位表示だけが担える。よって維持。実体は独立画面ではなく**再利用コンポーネント（領域）**で、
  返却タブと貸出フローの両方が同じ部品をホストする

### 追記10：適用範囲の前提と表示詳細の扱い（2026-06-16 同日）

- **学童保育ケース（オーナー気づき）**：子どもが自分で操作し、親に借りている本を
  知られたくないケースがありうる。ただし本製品の対象は保育園・幼稚園（操作者＝保護者）
  なので**今は設計しない**。代わりに「家庭内共有OK」という前提を SCREEN_DESIGN_PHASE2 に
  明文化し、学童展開時の再設計項目（家庭の画面・ひらがなモード・プライバシー境界）を列挙
- **一覧のセクション分け（園児/保護者）**：IAでなく表示詳細のため、実装時にPreviewで
  混在五十音順と見比べて決定（未決事項に選択肢を追加）
- **延滞書名が一覧で見えない件**：意図どおり（発見5の配慮の線）。先生の月次コストは
  数タップで許容。不満が出たら設定内俯瞰の復活という戻り先を記録

### 追記11：貸出の一時タスクはシートで出す（プッシュ遷移から変更・2026-07-04）

- **証拠（オーナー指摘・段取り3のシミュレータ確認）**：「だれが借りますか？」→枠確認は
  一時的な作業であり、Navigationで画面ごと乗り換えるのは重い。sheetか右サイドがよいのでは
- **決定**：本タップで**フォームシート**を開き、その中で「だれが借りますか？」→枠確認→
  ✓カード→シートが閉じる、まで完結させる。背後の図書一覧は一度も動かない
  - HIGの意味が一致：シート＝「自己完結した一時的なタスク」の受け皿
  - モーダルであることが行列ファーストに効く：背後の一覧が触れなくなり、
    急いでいる人が別の本を誤タップする導線が消える（原則2「ミスは前提」）
  - 置き去り復帰・完了復帰が「シートを閉じる」の1動作に単純化される
    （プッシュ遷移で必要だったpop＋スクロール先頭復帰の後始末が不要になる）
- **右サイドパネル（inspector）は不採用**：HIG上の意味は「選択中アイテムの詳細・属性の
  常設パネル」で、手続きを進める場所ではない。非モーダルで誤タップ導線が残り、
  幅約320ptは「1m離れて読める」タイポグラフィ方針（DESIGN_PRINCIPLES §5）と相性が悪い
- **ただしPOS（レジ）型なら右パネルは筋が通る**：デザイナー提案の「画像ベースのマス目」
  グリッドが主役になった場合、右側は『選択中の本の詳細』ではなく『進行中の取引エリア』
  という別の意味を獲得する（スーパーのレジ・POS端末の構図）。これは**Phase 3の設計スパイク**
  として、表紙グリッド＋右取引パネルのペーパープロトタイプで検証してから判断する。
  今の縦一覧に右パネルだけ足すと、狭い・非モーダル・意味が中途半端と弱点だけ背負う

### 追記12：「だれが借りますか？」から保護者を隠す（2026-07-04）

- **発端（オーナー相談）**：園児だけの一覧でよいのでは。ただし先生など大人が借りる
  ケースをシステムが想定しているなら残すべきか
- **証拠**：先生の利用ケースは実在する（オーナー現場知識）。ドメインも対応済み——
  `AgeGroup.other`（大人・その他）は「職員や特別な区分」用と明記されており、
  先生は大人の組に**保護者ではない独立利用者**として登録される
- **決定したルール**：一覧から隠すのは**「別の入口から構造的に到達できる利用者」だけ**
  - 保護者 → `guardian(relatedChildId:)` で必ず園児に紐づく＝園児タップ後の
    家庭の画面から枠を選べる。唯一の重複入口なので**隠す**
  - 園児・先生（独立利用者）→ 本人が唯一の入口なので**表示**（先生は「職員」等の組セクションに並ぶ）
- **効果**：家庭ごとの重複入口が消えて一覧が約半分になる。祖父母は保護者の登録名を
  知らなくても、孫の名前だけで借りられる（ペルソナ田中さん基準）
- **フォールバック**：紐づく園児が実在しない保護者（園児削除後の残留等のレアケース）は
  入口を失うため、その場合だけ本人を一覧に表示する
- **「園児だけ表示」にしなかった理由**：種別で絞ると先生が貸出フローから到達不能になる。
  絞り込みの根拠は種別ではなく「到達可能性」に置く

### 追記13：貸出フローの図書詳細・貸出中の扱い（2026-07-04・シミュレータレビュー）

- **図書の詳細・履歴には主動線から到達できない＝意図した設計として確定**
  （オーナー「ユースケースがないのでむしろない方がいい」）。消極的理由だけでなく
  積極的理由がある：**貸出履歴には他の家庭の名前が載る**ため、保護者が操作する
  キオスクの主動線に出すべきでない（返却一覧で書名を隠したのと同じプライバシーの線）。
  先生は絵本管理（管理系画面）から従来どおり閲覧できるので副シーンは欠けない
- **貸出中の図書はタップ可のまま**（非活性にしない）：無反応の行は田中さん基準で
  「壊れた？」になる（原則3違反）。タップ→「この図書は貸出中です」は行き止まりではなく案内
- **貸出中シートに返却予定日を表示**：「◯月◯日(土)ごろ返却予定です」。
  タップする人の本音「いつ戻る？」に答えることで、タップ可にした意味が生まれる。
  表示は日付のみで**誰が借りているかは出さない**。行側は「貸出中」バッジのままにして
  200冊の一覧のスキャンを軽く保つ（日付が一覧で欲しい証拠が出たら再検討）
- **シートはフォームシート→ページシートに変更**（`presentationSizing(.page)`）：
  「だれが借りますか？」が狭いというオーナー指摘。システム標準の器のまま一回り大きく

### 追記14：借りられる本の行に「借りる」ボタンを出す（2026-07-04）

- **発端（オーナー指摘）**：行がタップできると一目でわかるか不安
- **診断**：貸出タブの行はシートを開くButtonに変えたため、絵本管理のNavigationLinkに
  付いていたシェブロンが消え、「利用可」バッジも状態を言うだけで押せることを語らない＝
  押せるサインがゼロになっていた
- **決定**：借りられる本の右端を「利用可」バッジから**「借りる」ボタン**（絵本管理の
  貸出ボタン`LoanButtonView`と同じ青塗りの見た目）に置き換える。ボタンの形は
  押せることの世界共通のサインであり、動詞ファーストの原則（行が「押すと何が起きるか」を
  自分で説明する）にも一致。既存の絵本管理画面との見た目の一貫性も取れる（オーナー確認）
- 行全体のタップ判定は維持（ボタンの外を押しても同じ動き）。貸出中の本は
  「貸出中」バッジのまま（押せるが主役ではない温度感。タップすると返却予定日を案内）
- シェブロン案は不採用：「先に画面がある」の記号であって「押すと借りられる」までは
  語らず、高齢の操作者への伝達力が弱い

### 追記15：シートの大きさと組チップの文脈分岐（2026-07-04・シミュレータレビュー）

- **貸出中の案内シートは小さく（フォームシート）**：オーナー指摘「閉じるのがめんどくさい」。
  一言の案内にページシートは大きすぎる。中身の重さに器を合わせる
  （名前一覧を載せる貸出タスク＝ページシート、案内だけ＝フォームシート）
- **「だれが借りますか？」の組チップはフィルタ**（オーナー指摘「一覧すべてを見たい
  わけじゃなく切り替えたい」）。返却一覧のインデックス決定（追記・2026-07-03）と
  逆に見えるが、**画面の仕事が違う**：
  - 返却一覧＝探している名前が*どこにいるか分からない*画面。絞り込みで行が消えると
    「一覧に居ない」と勘違いする → インデックス
  - 貸出の利用者選択＝*自分の組が分かっていて*切り替えたい画面 → フィルタ（再タップで解除）
  - シートは本をタップするたびに作り直されるため絞り込みは毎回リセットされ、
    前の家庭の絞り込みが次の利用者に残らない
- `BorrowerListView` は `SectionChipBehavior`（scrollIndex／filter）で両文脈をホストする

### 追記16：返却一覧も家庭で1行に＋行の右端はボタンで揃える（2026-07-04）

- **返却一覧の保護者統合（オーナー提案）**：貸出フローで確立した「家庭のキーは園児名」を
  返却一覧にも適用する。ただし返却一覧は*貸出がある人*しか並ばないため、
  保護者を単純に隠すと園児に貸出がない家庭の入口が消えて返せなくなる。
  ルールは「隠す」ではなく「**保護者名義の貸出を紐づく園児（家庭の代表）の行に寄せる**」
  - 園児と保護者が両方借りている家庭が2行→1行にまとまる副次効果
  - 園児が見つからない保護者名義（削除後の残留等）は本人の行のまま（フォールバック）
  - 先生など独立した大人の名義は本人の行のまま
- **行の右端のデザイン統一（オーナー指摘）**：「貸出中」バッジは押せるように見えず、
  「借りる」ボタンと形も揃っていなかった。両方を**同じ形のボタン**にし、色で役割を分ける：
  青「借りる」＝主役の操作／グレー「貸出中」＝押すと返却予定日の案内（脇役の温度感）。
  形が同じ＝一覧で揃って見える、ボタン形＝押せることが伝わる、の両取り
